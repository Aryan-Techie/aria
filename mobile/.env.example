# Seeds the server address on first launch. Settings overrides it, and must,
# because the cloudflared quick tunnel mints a new hostname on every run.bat.
EXPO_PUBLIC_API_BASE=

# Reveals the rep screen from the bottom of Settings. Change it per demo.
EXPO_PUBLIC_REP_CODE=4821
